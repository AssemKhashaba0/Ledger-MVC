@{
    ViewData["Title"] = "الرئيسية";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="homepage-container">
    <section class="hero-section">
        <div class="hero-content">
            <h1 class="hero-title">سيبك من الورقة والقلم... وابدأ حساباتك صح!</h1>
            <p class="hero-subtitle">أسهل دفتر إلكتروني لإدارة العملاء والموردين والمستحقات</p>
            <p class="hero-description">من موبايلك أو لابتوبك – في أي وقت وأي مكان</p>

            <div class="hero-cta">
                <span class="price-highlight">بأقل من 7 جنيه في اليوم</span>
                <p class="cta-description">تحافظ على فلوسك وتنظم شغلك وتبقى دايمًا فاهم اللي ليك واللي عليك!</p>
                <a href="/Subscribe" class="btn-primary">اشترك الآن وابدأ حساباتك</a>
            </div>
        </div>
    </section>

    <section class="features-section">
        <div class="container">
            <h2 class="section-title">ليه تختار دفترك الإلكتروني؟</h2>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">
                        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#A78BFA" stroke-width="2">
                            <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2" />
                            <circle cx="9" cy="7" r="4" />
                            <path d="M23 21v-2a4 4 0 0 0-3-3.87" />
                            <path d="M16 3.13a4 4 0 0 1 0 7.75" />
                        </svg>
                    </div>
                    <h3 class="feature-title">إدارة العملاء والموردين</h3>
                    <p class="feature-description">أضف عملاءك وموردينك بسهولة وتابع كل التعاملات معاهم في مكان واحد</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#A78BFA" stroke-width="2">
                            <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6" />
                        </svg>
                    </div>
                    <h3 class="feature-title">حساب تلقائي لكل فلوسك</h3>
                    <p class="feature-description">النظام بيحسب كل حاجة تلقائيًا - مش محتاج تعمل أي حسابات بنفسك</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#A78BFA" stroke-width="2">
                            <path d="M3 3v18h18" />
                            <path d="M18.7 8l-5.1 5.2-2.8-2.7L7 14.3" />
                        </svg>
                    </div>
                    <h3 class="feature-title">تقارير يومية وشهرية</h3>
                    <p class="feature-description">شوف أرباحك وخسائرك بتقارير واضحة ومفصلة لكل فترة</p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">
                        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#A78BFA" stroke-width="2">
                            <path d="M9 12l2 2 4-4" />
                            <path d="M21 12c-1 0-3-1-3-3s2-3 3-3 3 1 3 3-2 3-3 3" />
                            <path d="M3 12c1 0 3-1 3-3s-2-3-3-3-3 1-3 3 2 3 3 3" />
                            <path d="M3 12c0 5.5 4.5 10 10 10s10-4.5 10-10" />
                        </svg>
                    </div>
                    <h3 class="feature-title">آمن – سريع – سهل</h3>
                    <p class="feature-description">بياناتك محمية بأعلى مستويات الأمان وسرعة في الاستخدام</p>
                </div>

                <div class="feature-card feature-card-highlight">
                    <div class="feature-icon">
                        <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#A78BFA" stroke-width="2">
                            <path d="M12 2L2 7v10c0 5.55 3.84 9.74 9 11 5.16-1.26 9-5.46 9-11V7l-10-5z" />
                            <path d="M9 12l2 2 4-4" />
                        </svg>
                    </div>
                    <h3 class="feature-title">مصمم للمشاريع الصغيرة</h3>
                    <p class="feature-description">مخصوص لأصحاب المشاريع الصغيرة والتجار - سهل ومش معقد</p>
                </div>
            </div>
        </div>
    </section>

    <section class="pricing-section">
        <div class="pricing-card">
            <div class="pricing-header">
                <h2 class="pricing-title">200 جنيه فقط شهريًا</h2>
                <p class="pricing-subtitle">يعني أقل من كوباية شاي في اليوم</p>
            </div>
            <div class="pricing-body">
                <a href="/Subscribe" class="btn-primary btn-large">اشترك الآن وابدأ حساباتك</a>
                <p class="pricing-note">بدون تجربة مجانية – بدون تعقيد – اشتراك مباشر</p>
            </div>
        </div>
    </section>

    <section class="contact-section">
        <div class="contact-content">
            <p class="contact-question">لسه مش واثق؟</p>
            <a href="https://wa.me/201000000000" target="_blank" class="contact-link">
                <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z" />
                </svg>
                ابعتلنا على واتساب وهنشرحلك في دقيقة
            </a>
        </div>
    </section>
</div>

<style>
    /* General Styles & Body */
    .homepage-container {
        background-color: #FAFAFA; /* Background */
        color: #1E1E1E; /* Text Main */
        min-height: 100vh;
        font-family: 'Cairo', sans-serif; /* استخدام خط عربي جذاب */
        line-height: 1.6;
        direction: rtl; /* للغة العربية */
        text-align: right; /* للغة العربية */
    }

    /* Importing Google Font (Cairo) */
    @@import url('https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap');

    /* Hero Section */
    .hero-section {
        padding: 5rem 1rem; /* زيادة الـ padding */
        text-align: center;
        background: linear-gradient(135deg, #FAFAFA 0%, #FFFFFF 100%); /* مزيج من Background و Surface */
        position: relative;
        overflow: hidden;
        border-bottom: 1px solid #E5E7EB; /* Border */
    }

        .hero-section::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0; /* تعديل ليتناسب مع RTL */
            bottom: 0;
            left: unset; /* تعديل ليتناسب مع RTL */
            background: radial-gradient(circle at 70% 20%, rgba(212, 175, 55, 0.1) 0%, transparent 50%); /* تأثير ذهبي خفيف */
            pointer-events: none;
            width: 100%;
            height: 100%;
        }

    .hero-content {
        max-width: 900px;
        margin: 0 auto;
        position: relative;
        z-index: 1;
    }

    .hero-title {
        font-size: clamp(2.2rem, 4.5vw, 3.5rem); /* تكبير الخط أكثر */
        font-weight: 700;
        margin-bottom: 1.5rem;
        color: #1E1E1E; /* Text Main */
        text-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }

    .hero-subtitle {
        font-size: clamp(1.2rem, 2.8vw, 1.6rem); /* تكبير الخط */
        color: #6B7280; /* Text Secondary */
        margin-bottom: 0.75rem;
        font-weight: 600; /* خط أثقل قليلاً */
    }

    .hero-description {
        font-size: clamp(1.1rem, 2.2vw, 1.4rem); /* تكبير الخط */
        color: #6B7280; /* Text Secondary */
        margin-bottom: 2.5rem;
    }

    .hero-cta {
        margin-top: 3rem; /* زيادة المسافة */
        background: #FFFFFF; /* Surface */
        padding: 2.5rem; /* زيادة الـ padding */
        border-radius: 16px;
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08); /* ظل أوضح */
        display: inline-block; /* لضمان أن الـ shadow يحيط بالمحتوى فقط */
        width: auto;
        max-width: 100%;
    }

    .price-highlight {
        font-size: clamp(1.5rem, 3.5vw, 2.2rem); /* تكبير الخط أكثر */
        color: #D4AF37; /* Primary */
        font-weight: 700;
        display: block;
        margin-bottom: 1rem;
        text-shadow: 0 2px 4px rgba(212, 175, 55, 0.4);
    }

    .cta-description {
        margin-bottom: 2rem;
        font-size: 1.2rem; /* تكبير الخط */
        color: #374151; /* لون قريب من Text Main للحصول على تباين جيد */
        max-width: 500px; /* لتقييد عرض النص */
        margin-left: auto; /* لوسيطة النص في RTL */
        margin-right: auto; /* لوسيطة النص في RTL */
    }

    .btn-primary {
        background: linear-gradient(135deg, #D4AF37 0%, #B8941F 100%); /* Primary */
        color: white;
        padding: 1.2rem 3rem; /* زيادة الـ padding */
        border-radius: 12px;
        text-decoration: none;
        font-weight: 600;
        font-size: 1.2rem; /* تكبير الخط */
        display: inline-block;
        transition: all 0.3s ease;
        box-shadow: 0 6px 20px rgba(212, 175, 55, 0.4); /* ظل أقوى */
        border: none;
        cursor: pointer;
    }

        .btn-primary:hover {
            transform: translateY(-3px); /* تأثير رفع أوضح */
            box-shadow: 0 8px 25px rgba(212, 175, 55, 0.5); /* ظل أكبر عند التحويم */
            background: linear-gradient(135deg, #B8941F 0%, #D4AF37 100%);
        }

    .btn-large {
        padding: 1.5rem 3.5rem; /* حجم أكبر للزر */
        font-size: 1.3rem;
    }

    /* Features Section */
    .features-section {
        padding: 6rem 1rem; /* زيادة الـ padding */
        background: #FFFFFF; /* Surface */
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
    }

    .section-title {
        text-align: center;
        font-size: clamp(2rem, 3.5vw, 2.8rem); /* تكبير الخط أكثر */
        margin-bottom: 4rem; /* زيادة المسافة */
        color: #1E1E1E; /* Text Main */
        font-weight: 700;
        position: relative;
    }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -15px; /* وضع الخط تحت العنوان */
            right: 50%; /* لتوسيط الخط في RTL */
            transform: translateX(50%); /* لضبط التوسيط */
            width: 80px;
            height: 4px;
            background-color: #D4AF37; /* Primary */
            border-radius: 2px;
        }

    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); /* تعديل الـ minmax للحصول على 3 أعمدة في المساحات الأكبر */
        gap: 2.5rem; /* زيادة المسافة بين البطاقات */
        margin-top: 3rem;
    }

    .feature-card {
        background: #FFFFFF; /* Surface */
        border: 1px solid #E5E7EB; /* Border */
        border-radius: 16px;
        padding: 2.5rem; /* زيادة الـ padding */
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        text-align: right; /* للغة العربية */
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05); /* ظل خفيف للبطاقات */
    }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0; /* تعديل ليتناسب مع RTL */
            left: unset; /* تعديل ليتناسب مع RTL */
            width: 4px; /* تغيير الشريط إلى الجانب الأيمن */
            height: 100%;
            background: linear-gradient(180deg, #A78BFA 0%, #D4AF37 100%); /* Accent و Primary */
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .feature-card:hover {
            transform: translateY(-8px); /* تأثير رفع أوضح */
            box-shadow: 0 15px 40px rgba(167, 139, 250, 0.2); /* ظل أكبر وأقوى */
            border-color: #A78BFA; /* Accent */
        }

            .feature-card:hover::before {
                opacity: 1;
            }

    .feature-card-highlight {
        background: linear-gradient(135deg, #F9FAFB 0%, #FAFAFA 100%); /* مزيج من Background و Surface */
        border-color: #D4AF37; /* Primary */
        box-shadow: 0 8px 25px rgba(212, 175, 55, 0.1); /* ظل ذهبي مميز */
    }

    .feature-icon {
        margin-bottom: 1.5rem;
        display: flex;
        align-items: center;
        justify-content: flex-end; /* ليتناسب مع RTL */
        margin-right: -10px; /* لتعويض المسافة */
    }

        .feature-icon svg {
            stroke: #A78BFA; /* Accent */
        }

    .feature-card-highlight .feature-icon svg {
        stroke: #D4AF37; /* Primary للبطاقة المميزة */
    }

    .feature-title {
        color: #1E1E1E; /* Text Main */
        font-size: 1.4rem; /* تكبير الخط */
        font-weight: 700;
        margin-bottom: 0.8rem;
    }

    .feature-description {
        color: #6B7280; /* Text Secondary */
        font-size: 1.05rem; /* تكبير الخط قليلاً */
        line-height: 1.8;
    }

    /* Pricing Section */
    .pricing-section {
        padding: 5rem 1rem;
        background: linear-gradient(135deg, #FAFAFA 0%, #F9FAFB 100%); /* مزيج من Background و Surface */
        border-top: 1px solid #E5E7EB; /* Border */
    }

    .pricing-card {
        background: #FFFFFF; /* Surface */
        border: 2px solid #E5E7EB; /* Border */
        max-width: 750px; /* عرض أكبر للبطاقة */
        margin: 0 auto;
        border-radius: 20px;
        padding: 3.5rem 2.5rem; /* زيادة الـ padding */
        text-align: center;
        box-shadow: 0 15px 50px rgba(0, 0, 0, 0.12); /* ظل أقوى */
        position: relative;
        overflow: hidden;
    }

        .pricing-card::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0; /* تعديل ليتناسب مع RTL */
            left: unset; /* تعديل ليتناسب مع RTL */
            width: 100%;
            height: 8px; /* شريط أعلى سميك */
            background: linear-gradient(90deg, #10B981 0%, #D4AF37 100%); /* Success و Primary */
        }

    .pricing-header {
        margin-bottom: 2.5rem;
    }

    .pricing-title {
        font-size: clamp(2rem, 4.5vw, 3rem); /* تكبير الخط أكثر */
        color: #10B981; /* Success */
        font-weight: 700;
        margin-bottom: 1rem;
    }

    .pricing-subtitle {
        color: #6B7280; /* Text Secondary */
        font-size: 1.2rem; /* تكبير الخط */
        margin-bottom: 0;
    }

    .pricing-body {
        margin-top: 2.5rem;
    }

    .pricing-note {
        margin-top: 1.5rem;
        color: #EF4444; /* Danger */
        font-weight: 600; /* خط أثقل */
        font-size: 1rem; /* تكبير الخط قليلاً */
    }

    /* Contact Section */
    .contact-section {
        padding: 4rem 1rem;
        text-align: center;
        background: #FFFFFF; /* Surface */
        border-top: 1px solid #E5E7EB; /* Border */
    }

    .contact-content {
        max-width: 600px; /* عرض أكبر لمحتوى التواصل */
        margin: 0 auto;
        background: #FAFAFA; /* Background */
        padding: 2rem;
        border-radius: 16px;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.06);
    }

    .contact-question {
        color: #6B7280; /* Text Secondary */
        font-size: 1.2rem; /* تكبير الخط */
        margin-bottom: 1.5rem;
        font-weight: 600;
    }

    .contact-link {
        color: #A78BFA; /* Accent */
        text-decoration: none;
        font-weight: 600;
        font-size: 1.2rem; /* تكبير الخط */
        display: inline-flex;
        align-items: center;
        gap: 0.75rem; /* زيادة المسافة بين الأيقونة والنص */
        transition: all 0.3s ease;
        padding: 0.8rem 1.8rem;
        border-radius: 10px;
        border: 1px solid #A78BFA; /* Border بنفس لون الـ Accent */
    }

        .contact-link svg {
            stroke: #A78BFA; /* Accent */
        }

        .contact-link:hover {
            background: rgba(167, 139, 250, 0.15); /* خلفية أغمق عند التحويم */
            transform: translateY(-3px); /* تأثير رفع أوضح */
            box-shadow: 0 6px 15px rgba(167, 139, 250, 0.25);
        }

    /* Responsive Adjustments */
    @@media (max-width: 768px) {
        .hero-section {
            padding: 4rem 1rem;
        }

        .hero-title {
            font-size: clamp(1.8rem, 5vw, 2.8rem);
        }

        .features-grid {
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); /* ضبط الأعمدة للشاشات الصغيرة */
            gap: 2rem;
        }

        .feature-card {
            padding: 2rem;
        }

        .pricing-card {
            padding: 3rem 1.5rem;
        }

        .features-section {
            padding: 4rem 1rem;
        }

        .section-title {
            font-size: clamp(1.8rem, 4vw, 2.5rem);
            margin-bottom: 3rem;
        }
    }

    @@media (max-width: 480px) {
        .features-grid {
            grid-template-columns: 1fr;
        }

        .feature-card {
            min-width: unset;
        }

        .hero-cta {
            padding: 1.5rem;
        }

        .btn-primary {
            padding: 1rem 2rem;
            font-size: 1.1rem;
        }

        .btn-large {
            padding: 1.1rem 2.2rem;
            font-size: 1.15rem;
        }

        .contact-link {
            font-size: 1rem;
            padding: 0.6rem 1.2rem;
        }
    }
</style>